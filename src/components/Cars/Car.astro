---
import Image from './Image.astro';
import Images from './Images.astro';
import Name from './Name.astro';

import './Car.css';

const { name, make, model, year, images } = Astro.props;
---
<div class="car">
  <Images>
    {images.map(({ path }) => (
      <Image name={name} make={make} model={model} imagePath={path} />
    ))}
  </Images>
  <Name name={name} year={year} />
</div>
