{
  "name": "@chealt/image-transformer",
  "main": "./src/index.js",
  "license": "MIT",
  "scripts": {
    "build": "zip -r build.zip . -x 'yarn.lock' -x 'pipeline*'",
    "deploy": "node ./pipeline/deploy.js",
    "release": "yarn build && yarn deploy"
  },
  "dependencies": {
    "@squoosh/lib": "^0.4.0",
    "aws-sdk": "^2.1018.0"
  },
  "devDependencies": {
    "@aws-sdk/client-lambda": "^3.39.0"
  }
}
